#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
using namespace std;

string intTOstring(long int a);

string addspace(string s);
string removespace(string s);
