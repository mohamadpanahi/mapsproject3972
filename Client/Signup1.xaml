<Page
    x:Class="testcsh.Signup1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:testcsh"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}">

    <Grid>
        <Button x:Name="btn_back" VerticalAlignment="Top" Style="{StaticResource NavigationBackButtonNormalStyle}" Click="Btn_back_Click" Margin="0,20,0,0"/>
        <StackPanel x:Name="st_main" FlowDirection="RightToLeft" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel x:Name="st_into" Orientation="Horizontal">
                <StackPanel x:Name="st_lbl">
                    <TextBlock Text="نام کاربری" TextWrapping="Wrap" Margin="0,0,0,12" Height="32"/>
                    <TextBlock Text="رمز عبور" TextWrapping="Wrap" Height="32"/>
                    <TextBlock Text="تکرار رمز عبور" TextWrapping="Wrap" Height="32" Margin="0,12,0,0"/>
                </StackPanel>
                <StackPanel x:Name="st_txt" Width="282" Margin="12,0,0,0">
                    <TextBox x:Name="txt_user" PlaceholderText="نام کاربری را وارد کنید"/>
                    <PasswordBox x:Name="txt_pass" Margin="0,12,0,0" PlaceholderText="رمز عبور را وارد کنید"/>
                    <PasswordBox x:Name="txt_confirmpass" Margin="0,12,0,0" PlaceholderText="رمز عبور را تکرار کنید"/>
                </StackPanel>
            </StackPanel>
            <Button x:Name="btn_continue" Content="ادامه" HorizontalAlignment="Center" Margin="0,11,0,0" Click="Btn_continue_Click"/>
        </StackPanel>
    </Grid>
</Page>
