1- in (generatecode && retrievepass) check connection to send email
4- how deleted account can active?
9- classification :O -> cleaning
10-baz negahdashtan query on server
13-search -- jsonpath : basic big search return (path + ' ' + js.dump() + "\n") instead (path + "\n")
17-فارسی نویسی و ارسال فارسی به cmd
18-